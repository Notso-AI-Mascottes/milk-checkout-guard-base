name: Untrusted PR precursor

on: pull_request

permissions:
  contents: read

jobs:
  hold:
    runs-on: ubuntu-latest
    steps:
      - name: Keep the run active for the deletion race
        run: |
          echo 'PRECURSOR_STARTED=true'
          sleep 45
          echo 'PRECURSOR_COMPLETED=true'
